<nav>
  <ul id="navbar">
    <li class="nav-item"><a class="active" href="#home">Book List</a></li>
    <li class="nav-item" style="float:right">
      <a href="#" data-toggle="dropdown" class="btn btn-primary dropdown-toggle get-started-btn mt-1 mb-1">Sign Up</a>

      <ul class="dropdown-menu form-wrapper">
        <li>
          <form id="signup-submit" method="post">
            <p class="hint-text">Fill in this form to create your account!</p>
            <div class="form-group">
              <input id="signup-email" type="text" class="form-control" placeholder="Username" required="required">
            </div>
            <div class="form-group">
              <input id="signup-password" type="password" class="form-control" placeholder="Password"
                required="required">
            </div>
            <div class="form-group">
              <input type="password" class="form-control" placeholder="Confirm Password" required="required">
            </div>
            <div class="form-group">
              <label class="checkbox-inline"><input type="checkbox" required="required"> I accept the <a href="#">Terms
                  &amp;
                  Conditions</a></label>
            </div>
            <input type="submit" class="btn btn-primary btn-block" value="Sign up">
          </form>
        </li>
      </ul>

    </li>
    <li id="sign-in-btn" class="nav-item" style="float:right">
      <a data-toggle="dropdown" class="nav-link dropdown-toggle" href="#">Login</a>

      <ul class="dropdown-menu form-wrapper">
        <li>
          <form id="login-submit" method="post">
            <div class="form-group">
              <input id="email-input" type="text" class="form-control" placeholder="Username" required="required">
            </div>
            <div class="form-group">
              <input id="password-input" type="password" class="form-control" placeholder="Password">
            </div>
            <input type="submit" class="btn btn-primary btn-block" value="Login">
            <div class="form-footer">
              <a href="#">Forgot Your password?</a>
            </div>
          </form>
        </li>
      </ul>

    </li>
  </ul>
</nav>
<div id="wrapper">
  <h1 id="heading">My Reading List</h1>
  <div id="user-input">
    <form id="add-book">
      <input name="book" id="book"></input>
      <input type="submit" value="Read This!"></input>
    </form>
  </div>

  <div id="content">
    <div id="to-read" class="db-items">
      <ul id="read-list">
        {{#each books}}
        {{#unless completed}}
        <li>
          <p class="book listed-book" data-val="{{this}}" title="{{this.author}}">
            <button data-id="{{this.id}}" class="delete-book">&#10060;</button>
            {{this.title}}
            <span class="author">By: {{this.author}}</span>
            <button data-id="{{this.id}}" class="finished-book">&#10003;</button>
          </p>
        </li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div id="completed" class="db-items">
      <ul id="finished-list">
        {{#each books}}
        {{#if this.completed}}
        <li>
          <p class="book read-book" data-val="{{this}}" title="{{this.author}}">
            <button data-id="{{this.id}}" class=" delete-book">&#10060;</button>
            {{this.title}}
            <span class="author">{{this.author}}</span>
          </p>
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>
<footer>
  <ul>
    <li><a class="footer-link" href="https://github.com/Gnarwall19">GitHub</a></li>
    <li><a class="footer-link" href="https://www.linkedin.com/in/michael-c-wolfe/">linkedIn</a></li>
    <li><a class="footer-link" href="https://gnarwall19.github.io/michael-wolfe-portfolio/">Portfolio</a></li>
    <li><input id="logout" data-id="{{this}}" type="submit" value="Log Out"></li>
  </ul>
</footer>

<script>
  // Prevent dropdown menu from closing when click inside the form
  $(document).on("click", ".dropdown-menu", function (e) {
    e.stopPropagation();
  });
</script>